/**
 * conversation.ts
 * Purpose: Defines cross-layer shared conversation types (types only; no runtime code).
 * Created: 2026-03-09
 */

export type Conversation = {
  id: string;
  href: string;
  title: string;
};

